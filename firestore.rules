rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Day 4: keep Firestore closed until real data flows and auth checks exist.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
